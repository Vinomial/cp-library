# このリポジトリでの役割

競技プログラミング用の個人ライブラリ（[README.md](./README.md) 参照）。ここでの作業は主に、
アルゴリズム・データ構造を実装して `cpp/`（+ `python/`）に追加する／整理する／ドキュメント
（`.md`）を整えることが中心になる。第三者コードのライセンス判断・履歴の扱いなど運用上の
手順は [MAINTENANCE.md](./MAINTENANCE.md) にまとまっているので、それとは重複させず、ここでは
これまでの作業を通して得られた「実装・検証の進め方」を書く。

## ライブラリ設計の指針

- 1データ構造・1アルゴリズムにつき `{name}.hpp` + `{name}.md`（`documentation_of` フロント
  マター付き）。名前空間は基本 `{何か}_lib`（例: `trie_lib`, `graph_lib`）。
- 「最小限の骨組み」を志向する。集合としての基本操作（挿入・削除・検索・存在確認など、
  中身の順序に依存しない操作）はライブラリに持たせるが、問題固有の探索ロジック（辞書順
  ランク計算、01-trie上のXOR最大化 等、要素の順序に意味がある操作）は `node()` のような
  アクセサ経由で呼び出し側に書かせる（例: [trie.md](./cpp/string/trie.md)）。新しい操作を
  足すか迷ったら「アルファベットの順序を変えても意味が変わらないか」を判断基準にする。
- 迷いどころ（bool→カウントに変えるか、新規関数を足すか 等）はユーザーに選択肢を示して
  確認してから実装する。「入れない」判断も実装と同じくらい積極的にドキュメント化する
  （検討したが見送った理由を `.md` に残す）。

## AtCoder Library (ACL) から移植するとき

`vendor/ac-library` に本家がそのまま入っている。ACLベースで `cpp/` に実装を足すときは：

- **公開APIの名前・シグネチャはACLのものをできるだけ維持する**（`add_edge` / `flow` /
  `edge` 構造体 等）。ACLに慣れているユーザーがそのまま使える方が価値が高い。
- スタイルだけをこのリポジトリに合わせる：`{何か}_lib` 名前空間、非公開メンバは
  trailing underscore（`n_`, `g_`）、事前条件は `assert`、コメントは日本語で一行程度。
- テンプレート引数にはこのリポジトリの前例に合わせたデフォルトを与える
  （`WeightedUnionFind<W = long long>` に倣い `mf_graph<Cap = long long>` 等）。
- ACL内部の補助型（`internal::simple_queue` 等）は、リポジトリ内に代替の前例が
  あればそちらに寄せる（`topological_sort.hpp` が `std::queue` を使っている、など）。
  新規に依存を増やさない。
- 実装後は **`vendor/ac-library` の本物と直接differential testする**（ランダムグラフ・
  境界ケース・自己ループ/多重辺などを含めて多数ケース比較）。可能なら、関連する
  過去の自分の解答（`cp-solutions/`）とも突き合わせる。

## 検証の進め方（コミット前に必須）

1. スクラッチパッドに使い捨てのテスト `.cpp` を書く（本体のコミットには含めない）
2. `g++ -std=c++17 -Wall -Wextra`（性能・大規模ケースを見るときは `-O2` も）でコンパイル
3. 愚直解との比較・既知のACL実装との差分テスト・境界値／異常系（assertが正しく
   発火するか）を確認する
4. スクラッチパッドのテストファイルは確認後に削除し、コミットには本体（`.hpp`/`.md`）
   だけを含める

## ドキュメントの同期を忘れない

`.hpp` を追加・リネームしたら、**カテゴリの `README.md`（例: `cpp/graph/README.md`）と
最上位の `cpp/README.md` の両方**を更新する。過去に `z_algorithm.md` と、追加直後の
`max_flow.md` でこの最上位側の追記を一度忘れているので、片方だけ直して終わりにしない
ことを明示的に意識する。

## `vendor/` を更新するとき

- `vendor/ac-library` は本家のディレクトリ構成をそのまま踏襲する（`atcoder/` ヘッダー、
  `LICENSE`、`document_ja/` 等）。何を含める／含めないかの方針は
  [vendor/ac-library/README.md](./vendor/ac-library/README.md) に書いてあるので、
  方針を変える更新をしたら README も同時に書き換える。
- ユーザーが貼り付けた外部由来の資料（HTML化されたドキュメント等）をそのまま信用しない。
  実際に、渡された13個のHTMLファイルの**ファイル名と中身が全部シャッフルされていた**
  ことがあった。ページ自身の内容（`<h1>` 等）や、他ページに埋め込まれたナビゲーション
  リンクなど、ファイル名以外の手がかりで正しい対応を確認してから配置する。

## コミット運用

- 個人用リポジトリで、PRを介さず `main` に直接コミット・pushする運用になっている。
- 1つの自己完結した変更につき1コミット。コミットメッセージには変更理由と、
  何を検証したか（差分テストの規模・対象など）を書く。
